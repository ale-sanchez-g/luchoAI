# Anthropic API key for local development
# Users can also enter it via the app UI — it's stored in localStorage
NEXT_PUBLIC_ANTHROPIC_API_KEY=

# GitHub Pages base path — set automatically in CI deploy workflow
# Leave empty for local development
NEXT_PUBLIC_BASE_PATH=
